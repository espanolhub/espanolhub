# 🗄️ دليل إعداد قاعدة البيانات Supabase

## ✅ ما تم إنجازه

1. ✅ إنشاء `prisma/schema.prisma` - Schema للمستخدمين
2. ✅ إنشاء `lib/db.ts` - Prisma Client singleton
3. ✅ تحديث `lib/admin-users.ts` - استخدام Prisma بدلاً من array محلي
4. ✅ تحديث API routes - استخدام async functions
5. ✅ إنشاء `prisma/seed.ts` - script لإضافة المستخدمين الافتراضيين

---

## 📋 الخطوات التالية (في Terminal)

### الخطوة 1: تثبيت tsx (للتشغيل scripts)

```bash
npm install -D tsx
```

### الخطوة 2: إضافة DATABASE_URL محلياً

في `.env.local` (إذا لم يكن موجوداً):

```env
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres"
```

**⚠️ مهم:** استبدل `YOUR_PASSWORD` و `xxxxx` بالقيم الفعلية من Supabase.

### الخطوة 3: إنشاء الجداول في قاعدة البيانات

```bash
npx prisma migrate dev --name init
```

هذا الأمر سينشئ:
- جدول `users` في قاعدة البيانات
- ملف migration في `prisma/migrations/`

### الخطوة 4: توليد Prisma Client

```bash
npx prisma generate
```

### الخطوة 5: إضافة المستخدمين الافتراضيين

```bash
npm run db:seed
```

أو:
```bash
npx tsx prisma/seed.ts
```

---

## ✅ التحقق من النجاح

### 1. تحقق من الجدول في Supabase:

1. اذهب إلى Supabase Dashboard
2. Database → Tables
3. يجب أن ترى جدول `users`

### 2. تحقق من المستخدمين:

في Supabase Dashboard:
- Database → Tables → users → View data
- يجب أن ترى المستخدمين الاثنين

### 3. جرب تسجيل الدخول:

- اذهب إلى `/login`
- جرب تسجيل الدخول بالمستخدمين الحاليين
- يجب أن يعمل!

---

## 🔧 أوامر مفيدة

```bash
# إعداد قاعدة البيانات
npm run db:migrate

# توليد Prisma Client
npm run db:generate

# إضافة بيانات أولية
npm run db:seed

# فتح Prisma Studio (واجهة مرئية)
npm run db:studio
```

---

## ⚠️ ملاحظات مهمة

1. **DATABASE_URL:**
   - يجب أن يكون موجوداً في `.env.local` (محلياً)
   - يجب أن يكون موجوداً في Vercel Environment Variables

2. **كلمات المرور:**
   - حالياً غير مشفرة (plain text)
   - TODO: إضافة bcrypt لاحقاً

3. **البيانات الحالية:**
   - المستخدمون الحاليون سيُضافون تلقائياً عند تشغيل `db:seed`
   - إذا كانوا موجودين، سيتم تخطيهم

---

## 📊 ما بعد الإعداد

بعد إكمال الخطوات:
- ✅ قاعدة البيانات تعمل
- ✅ المستخدمون محفوظون في السحابة
- ✅ Backup تلقائي
- ✅ جاهز للتوسع

---

**تاريخ الإنشاء:** 21 يناير 2026  
**الحالة:** جاهز للتنفيذ ✅
