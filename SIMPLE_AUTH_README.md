# 🔐 نظام المصادقة البسيط (بدون Clerk)

تم إنشاء نظام مصادقة بسيط وآمن يعمل بدون Clerk للوصول إلى لوحة التحكم.

## 👤 بيانات تسجيل الدخول

### المستخدم 1:
- **Email:** `esconabdou@gmail.com`
- **Password:** `Esconabdou123`

### المستخدم 2:
- **Email:** `boutibderrahim@gmail.com`
- **Password:** `Boutibderrahim123`

---

## 🚀 كيفية الاستخدام

### 1. تسجيل الدخول:
1. اذهب إلى: `/login`
2. أدخل البريد الإلكتروني والكلمة السرية
3. اضغط "Iniciar Sesión"
4. سيتم توجيهك تلقائياً إلى `/admin`

### 2. الوصول إلى Admin Panel:
- بعد تسجيل الدخول، يمكنك الوصول إلى `/admin`
- إذا حاولت الوصول إلى `/admin` بدون تسجيل دخول، سيتم توجيهك تلقائياً إلى `/login`
- ستجد جميع أدوات الإدارة المتاحة

### 3. تسجيل الخروج:
- اضغط على زر "Cerrar Sesión" في أعلى الصفحة في Admin Panel

---

## 📁 الملفات الجديدة

### API Routes:
- `app/api/auth/login/route.ts` - تسجيل الدخول
- `app/api/auth/logout/route.ts` - تسجيل الخروج (تم إصلاح اسم الـ cookie)
- `app/api/auth/me/route.ts` - التحقق من المستخدم الحالي

### Pages:
- `app/login/page.tsx` - صفحة تسجيل الدخول (تدعم redirect parameters)

### Libraries:
- `lib/auth.ts` - وظائف المصادقة
- `lib/hooks/useAuth.ts` - React hook للمصادقة
- `lib/admin-users.ts` - بيانات المستخدمين

### Middleware:
- `middleware.ts` - حماية صفحات Admin من الوصول غير المصرح

### Updated:
- `app/admin/page.tsx` - يعمل الآن بدون Clerk
- `app/layout.tsx` - يعمل مع أو بدون Clerk
- `components/Navigation.tsx` - يعمل بدون Clerk
- `components/SignUpIntentHandler.tsx` - يعمل مع أو بدون Clerk

---

## 🔒 الأمان

### الميزات الأمنية المطبقة:
- ✅ HTTP-only cookies لمنع XSS attacks
- ✅ Session expiration (7 أيام)
- ✅ Middleware لحماية صفحات Admin
- ✅ التحقق من الصلاحيات (Admin role)
- ✅ Secure cookies في الإنتاج (HTTPS)
- ✅ SameSite cookie attribute

### ⚠️ ملاحظات أمنية للإنتاج:
- النظام الحالي بسيط ومناسب للتطوير والاستخدام الشخصي
- كلمات المرور حالياً غير مشفرة (في ملف `lib/admin-users.ts`)
- للاستخدام في الإنتاج على نطاق واسع، يُنصح بـ:
  - تشفير كلمات المرور باستخدام bcrypt
  - استخدام قاعدة بيانات آمنة بدلاً من ملف JSON
  - إضافة rate limiting لمنع brute force attacks
  - استخدام HTTPS فقط
  - إضافة two-factor authentication (2FA)
  - تسجيل محاولات تسجيل الدخول الفاشلة

---

## ✅ الميزات

- ✅ تسجيل دخول بسيط بالبريد والكلمة السرية
- ✅ جلسات آمنة باستخدام HTTP-only cookies
- ✅ حماية صفحات Admin باستخدام Middleware
- ✅ تسجيل خروج آمن
- ✅ دعم redirect بعد تسجيل الدخول
- ✅ Session expiration (7 أيام)
- ✅ يعمل بدون Clerk (لا تكاليف إضافية)
- ✅ واجهة مستخدم جميلة وسهلة الاستخدام

---

## 🧪 الاختبار

### لاختبار النظام:
1. افتح المتصفح واذهب إلى `/admin`
2. يجب أن يتم توجيهك إلى `/login`
3. أدخل بيانات تسجيل الدخول
4. يجب أن يتم توجيهك إلى `/admin` بعد تسجيل الدخول بنجاح
5. اضغط على "Cerrar Sesión" للتأكد من عمل تسجيل الخروج

---

## 🔄 إذا أردت العودة إلى Clerk:

1. أزل ملفات المصادقة البسيطة:
   - `app/api/auth/`
   - `lib/auth.ts`
   - `lib/hooks/useAuth.ts`
   - `middleware.ts`
2. أعد تفعيل Clerk في:
   - `app/layout.tsx` (قم بإزالة الشرط وإبقاء ClerkProvider فقط)
   - `components/Navigation.tsx` (قم بإعادة استخدام useUser() و useAuth())
   - `app/admin/page.tsx` (قم بإعادة استخدام Clerk hooks)

---

## 📝 ملاحظات إضافية

- النظام يدعم عدة مستخدمين admins
- يمكن إضافة مستخدمين جدد بسهولة في `lib/admin-users.ts`
- الجلسات تستمر لمدة 7 أيام
- Cookie name: `admin-auth`

---

**تاريخ الإنشاء:** 21 يناير 2026  
**آخر تحديث:** 21 يناير 2026