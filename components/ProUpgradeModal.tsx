'use client';

export default function ProUpgradeModal({
  open: _open,
  onClose: _onClose,
  onGetPro: _onGetPro,
}: {
  open: boolean;
  onClose: () => void;
  onGetPro: () => void;
}) {
  // DISABLED - All content is free now
  return null;
}
