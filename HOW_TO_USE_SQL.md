# 📋 كيفية استخدام SQL مباشرة في Supabase

## ✅ الحل البديل السريع

بدلاً من استخدام Prisma Migration، يمكنك نسخ ولصق SQL مباشرة في Supabase!

---

## 🚀 الخطوات (3 دقائق):

### الخطوة 1: افتح Supabase SQL Editor

1. اذهب إلى: https://supabase.com/dashboard
2. اختر مشروعك
3. اضغط على **SQL Editor** من القائمة الجانبية
4. اضغط **New Query**

### الخطوة 2: انسخ والصق الكود

1. افتح ملف `supabase-setup.sql` في مشروعك
2. انسخ **جميع** محتويات الملف (Ctrl+A ثم Ctrl+C)
3. الصق في Supabase SQL Editor (Ctrl+V)

### الخطوة 3: شغّل الكود

1. اضغط **Run** (أو Ctrl+Enter)
2. انتظر حتى يكتمل التنفيذ
3. يجب أن ترى رسالة نجاح

---

## ✅ التحقق من النجاح:

بعد تشغيل SQL، شغّل هذا الاستعلام للتحقق:

```sql
SELECT * FROM "users";
```

يجب أن ترى مستخدمين:
- esconabdou@gmail.com (admin)
- boutibderrahim@gmail.com (admin)

---

## 🔐 بيانات تسجيل الدخول:

### المستخدم الأول:
- **Email:** `esconabdou@gmail.com`
- **Password:** `Esconabdou123`
- **Role:** `admin`

### المستخدم الثاني:
- **Email:** `boutibderrahim@gmail.com`
- **Password:** `Boutibderrahim123`
- **Role:** `admin`

---

## 🎯 الآن جرب:

1. افتح: http://localhost:3000/login
2. أدخل:
   - Email: `esconabdou@gmail.com`
   - Password: `Esconabdou123`
3. يجب أن تعمل! ✅

---

## 📝 ملاحظات:

- ✅ لا حاجة لـ Prisma Migration
- ✅ لا حاجة لـ `npm run db:migrate`
- ✅ يعمل مباشرة في Supabase
- ✅ كلمات المرور مشفرة مسبقاً

---

## 🔄 إذا أردت إضافة مستخدم جديد:

```sql
-- استخدم bcrypt generator: https://bcrypt-generator.com/
-- أدخل كلمة المرور واحصل على hash

INSERT INTO "users" ("id", "email", "password", "role", "username", "createdAt", "updatedAt")
VALUES (
  'clx' || substr(md5(random()::text || clock_timestamp()::text), 1, 10),
  'newuser@example.com',
  '$2a$10$...',  -- الصق الـ hash هنا
  'user',  -- أو 'admin'
  'newuser',
  NOW(),
  NOW()
);
```

---

## 🆘 إذا واجهت مشكلة:

### خطأ: "relation users already exists"
**الحل:** الجدول موجود بالفعل. استخدم:
```sql
DROP TABLE IF EXISTS "users" CASCADE;
```
ثم شغّل الكود مرة أخرى.

### خطأ: "duplicate key value"
**الحل:** المستخدم موجود بالفعل. الكود يستخدم `ON CONFLICT DO NOTHING` لذلك لن يحدث خطأ.

---

**تم! الآن النظام جاهز للاستخدام! 🎉**
